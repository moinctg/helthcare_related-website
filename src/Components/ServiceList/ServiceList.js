import React from 'react';

const ServiceList = (props) => {
    console.log(props.service)
    return (
        <div>
            <h3>here show all service list</h3>
        </div>
    );
};

export default ServiceList;